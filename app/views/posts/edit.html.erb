<h1>Editing Book</h1>
<%= form_for(@post) do |f| %>
    <h4>Title</h4>
    <%= f.text_field :title %>
    <h4>Body</h4>
    <%= f.text_area :body %><br>
    <%= f.submit 'Update Book' %><br>
    <%= link_to "Show", show_post_path(@post.id) %>|
    <%= link_to "back", posts_new_path %>
<% end %>